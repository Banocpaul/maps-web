@extends('layouts.app')
@section('title', $fireHydrant->hydrant_code . ' | M.A.P.S.')
@section('content')
<div class="mx-auto max-w-4xl space-y-6"><header class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-xs font-semibold uppercase tracking-wide text-sky-700">Fire Hydrant</p><h1 class="mt-1 text-2xl font-bold text-slate-950">{{ $fireHydrant->hydrant_code }}</h1><p class="mt-1 text-sm text-slate-600">{{ $fireHydrant->barangay?->name }} · {{ $fireHydrant->location }}</p></div><div class="flex gap-2"><a href="{{ route('fire-hydrants.index') }}" class="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700">Back</a>@if(auth()->user()?->hasPermission('hydrants.manage'))<a href="{{ route('fire-hydrants.edit', $fireHydrant) }}" class="rounded-xl bg-sky-700 px-4 py-2.5 text-sm font-semibold text-white">Edit</a>@endif</div></header><section class="grid gap-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2">@foreach(['Status' => $fireHydrant->status, 'Coordinates' => ($fireHydrant->latitude && $fireHydrant->longitude ? $fireHydrant->latitude.', '.$fireHydrant->longitude : 'Not recorded'), 'Installation Date' => $fireHydrant->installation_date?->format('M j, Y') ?? 'Not recorded', 'Last Inspection' => $fireHydrant->last_inspection_date?->format('M j, Y') ?? 'Not recorded', 'Remarks' => $fireHydrant->remarks ?: 'None'] as $label => $value)<div @class(['sm:col-span-2' => $label === 'Remarks'])><p class="text-xs font-semibold uppercase tracking-wide text-slate-500">{{ $label }}</p><p class="mt-2 text-sm font-medium text-slate-900">{{ $value }}</p></div>@endforeach</section></div>
@endsection
